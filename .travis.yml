language: python
python:
  - "2.7"
sudo: required
# command to install dependencies
install:
  - "sudo apt-get install python-pygame"
  - "pip install -r requirements.txt"
# command to run tests
script: "python -m doctest -v OctoPiControlPanel.py"